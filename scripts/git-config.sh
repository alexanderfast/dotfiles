#!/bin/bash
# TODO find a better way to keep this git configs in dotfiles
git config --global user.name "Alexander Fast"
git config --global merge.tool nvimdiff  
git config --global merge.conflictstyle diff3
