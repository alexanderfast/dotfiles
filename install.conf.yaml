- create:
        - ~/.config

- link:
        ~/.config/nvim: nvim
        ~/.oh-my-zsh: ohmyzsh
        ~/.tmux:
        ~/.tmux.conf:
        ~/.zsh:
        ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [~/.tmux/plugins/tpm/bin/install_plugins, Installing tmux plugins]
  - [~/dotfiles/nvim/install_plugins, Installing nvim plugins]
